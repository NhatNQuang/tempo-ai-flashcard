Underline tabs for in-page section switching. Gradient indicator under the active tab; optional count pills.

```jsx
<Tabs items={[{id:'overview',label:'Overview'},{id:'cards',label:'Flashcards',count:128}]}
      defaultValue="overview" onChange={setTab} />
```
