Toggle for binary settings and filters (e.g. "Verified only"). Knob springs on change; track turns violet when on.

```jsx
<Switch defaultChecked label="Verified only" />
<Switch size="sm" checked={dark} onChange={setDark} />
```
